// What does the following code log to the console?

function scream(words) {
  return words + '!!!!';
}

scream('Yipeee');

// The above code does not log anything to the console because the
// `log` method is never called on the `console`.