// In the code shown below, identify the following items:

function multiplyNumbers(num1, num2, num3) {
  let result = num1 * num2 * num3;
  return result;
}

let product = multiplyNumbers(2, 3, 4);

// - the function arguments
//   - The integers `2`, `3`, and `4` on `line `8`
// - the function body
//   - The area within the squiggley brackets, starting on `line 3`
//     and going to `line 6`.
// - the function declaration
//   - All lines of text including the keyword `function` on
//     `lines 3-6`
// - the function invocation
//   - `multiplyNumbers(2, 3, 4)` on `line 8`
// - the function name
//   - `multiplyNumbers`
// - the function parameters
//   - `num1`, `num2`, `num3` on `line 3`
// - the function return value
//   - The integer `24` will be stored in the variable, `result`,
//     which will be the return value of running the function.
//   - On `line 8`, the return value is stored in the
//     variable `product`.
// - the names of all variables in this program
//   - `multiplyNumbers`
//   - `num1`
//   - `num2`
//   - `num3`
//   - `result`
//   - `product`